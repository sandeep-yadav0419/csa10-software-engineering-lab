# Library Management System

## Purpose
This page connects the one week library sprint to its Jira work items.
The goal is member login, catalogue search, issue, return and borrowing history.

## Sprint scope
Use the five records in experiment 04. Each issue should contain its acceptance
criteria and an estimate. Select the real Jira project and sprint after import.

## Live issue list
Insert the Jira Issues macro using `/jira`. Select the current Jira work item
macro, or Jira Legacy if that is the macro available in your workspace.
Use `project = LIB ORDER BY status ASC, priority DESC` after replacing `LIB`
with the actual project key. Show Key, Summary, Status, Assignee and Priority.
Keep this macro connected to Jira rather than pasting a static screenshot table.

## Review
Open an issue, change its status, then refresh the Confluence page and confirm
the displayed status changes. Capture the page with the live macro and the issue key.

## Observed result
The page content and query are prepared. A live Jira integration has not been
executed here; the connected workspace and a real screenshot are still required.
